// Assuming this is the corrected content

// Other code above

someOuterArray.forEach(outerElement => {
    // ... some logic

    someInnerArray.forEach(innerElement => {
        // lines 52-54 corrected to proper indentation
        console.log(innerElement);
        // other logic
    });
});

// Other code below
